"use strict";

/**
 * Class used for creating observer entities.
 */

module.exports = class Observer{

    /**
     * Does not have any particular function for the
     * current time being.
     *
     * @return void
     */

    constructor(){


    }

    /**
     * Receives an update from a particular observer manager
     * and what it does with it, depends on the
     * context where it is used.
     *
     * @param update
     */

    getUpdate(update){


    }
};